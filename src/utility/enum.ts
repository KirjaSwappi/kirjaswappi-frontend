export enum FilterItemEnum {
  CATEGORY = 'CATEGORY',
  FILTER = 'FILTER',
  SORTBY = 'SORTBY',
}

export enum SortByEnum {
  title = 'title',
  author = 'author',
  language = 'language',
  condition = 'condition',
}
